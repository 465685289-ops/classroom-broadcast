# 作文学习助手部署（xiezuo 子域名）

目标地址：`https://xiezuo.yingyuzuowen.asia`

## 1. DNS

在阿里云 DNS 为 `yingyuzuowen.asia` 新增 A 记录：

- 主机记录：`xiezuo`
- 记录值：当前 Node 服务所在服务器的公网 IP

## 2. 服务器配置

先备份运行数据，再上传本次涉及的文件：

```bash
cd $APP_DIR
cp broadcast.db backups/broadcast.db.$(date +%Y%m%d-%H%M%S)
cp server.js backups/server.js.$(date +%Y%m%d-%H%M%S)
cp db.js backups/db.js.$(date +%Y%m%d-%H%M%S)
```

服务器的 `essay-config.json` 增加，不要把密钥提交进仓库：

```json
{
  "learning_base_url": "https://xiezuo.yingyuzuowen.asia"
}
```

该服务复用现有 `qwen_api_key`、`minimax_api_keys` 与云狗支付配置；AI 密钥只留在服务器。

> 本次新增「打卡 / 成就 / 个人中心 / 金句本 / 生成记录」功能，涉及 `db.js`、`server.js`、`public/xiezuo.html` 及新增 `public/vendor/confetti.browser.min.js`。新增数据表（learning_saved / learning_history / learning_checkins）在服务重启时由 `CREATE TABLE IF NOT EXISTS` 自动建好，无需手动迁移。

## 3. Nginx 与 HTTPS

为新子域名创建与现有 Node 服务相同的反向代理，必须保留上传图片所需的请求体限制：

```nginx
server {
    listen 443 ssl http2;
    server_name xiezuo.yingyuzuowen.asia;
    client_max_body_size 10m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

申请该子域名的 HTTPS 证书后，执行 `nginx -t && systemctl reload nginx`。

## 4. 启动与验收

```bash
systemctl restart classroom-broadcast
curl -s https://xiezuo.yingyuzuowen.asia/ | grep -c '作文学习助手'
curl -s https://xiezuo.yingyuzuowen.asia/api/learning/config
```

验收标准：

1. 未登录访问配置接口返回“请先登录”。
2. 新注册账号显示 7 天体验。
3. 体验结束后，学习接口拒绝请求并显示续费入口。
4. ¥9.90 学期卡和 ¥17.80 年卡支付成功后，会员有效期延长。
5. 在 `https://notice.yingyuzuowen.asia/admin.html` 对任意用户续费 180 或 365 天后，`xiezuo` 立即生效。
6. `notice`、`comment`、`zuowen` 三个既有地址仍返回 200。
7. 登录后「个人中心」可正常打卡（+50 经验、当天幂等）、查看等级与成就；生成结果可收藏进「金句本」；「生成记录」能回顾历史并一键载入工作台。
