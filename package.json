{
  "name": "classroom-broadcast",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js tests/*.test.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "express": "^4.21.0",
    "nodemailer": "^8.0.10",
    "socket.io": "^4.8.0"
  }
}
