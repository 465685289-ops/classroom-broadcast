# 作文批改助手上线部署（zuowen 子域名）

本次更新 3 个代码文件 + 1 个新配置文件，需要重启服务。

- `server.js`（新增 /api/essay/* 接口、zuowen 子域名路由、支付到账分支；express.json 上限提高到 10mb）
- `db.js`（新增 essay_credit_ledger / essay_gradings / essay_cards 三张表，启动时自动建表，老数据不受影响）
- `public/zuowen.html`（新前端，无任何明文 key）
- `essay-config.json`（新建，只放服务器，不进 git）

## 0. 前置条件（部署前必须完成）

1. **作废旧 key**：阿里云百炼、MiniMax 控制台里把旧 key 全部删除/禁用（已随旧版 HTML 泄露）。DeepSeek 检查服务器上 comment-config.json 用的 key 是否与泄露的 `sk-6e34...` 相同，相同则一并换。
2. **申请新 key**：通义千问（百炼）1 个、MiniMax 1-3 个。
3. **DNS**：阿里云域名解析控制台给 `yingyuzuowen.asia` 加一条 A 记录：主机记录 `zuowen`，记录值 = 服务器公网 IP。

## 1. 备份

```bash
cd $APP_DIR
cp server.js releases-backup/server.js.$(date +%Y%m%d-%H%M%S)
cp db.js releases-backup/db.js.$(date +%Y%m%d-%H%M%S)
cp broadcast.db releases-backup/broadcast.db.$(date +%Y%m%d-%H%M%S)
```

## 2. 上传 3 个文件

```bash
scp server.js db.js user@SERVER:$APP_DIR/
scp public/zuowen.html user@SERVER:$APP_DIR/public/
```

## 3. 服务器上创建 essay-config.json

```bash
cat > $APP_DIR/essay-config.json << 'EOF'
{
  "essay_base_url": "https://zuowen.yingyuzuowen.asia",
  "qwen_api_key": "新申请的通义千问key",
  "minimax_api_keys": "新key1,新key2"
}
EOF
chmod 600 $APP_DIR/essay-config.json
```

## 4. nginx 增加 zuowen 子域名

在现有 nginx 配置里照抄 comment.yingyuzuowen.asia 的 server 块，把 server_name 换成 `zuowen.yingyuzuowen.asia`，反代同一个 127.0.0.1:3000。**注意加上 `client_max_body_size 10m;`**（OCR 上传图片需要，nginx 默认 1m 会报 413）。

HTTPS 证书：用 certbot 或阿里云免费证书给 zuowen 子域名签一张（与 comment 当时的做法相同）。

```bash
nginx -t && nginx -s reload
```

## 5. 重启 Node 服务

```bash
# systemd 部署则：
systemctl restart classroom-broadcast
```

## 6. 验证

```bash
# 页面（期望 200，且包含"作文批改助手"）
curl -s https://zuowen.yingyuzuowen.asia/ | grep -c "作文批改助手"

# 未登录请求接口（期望 {"error":"请先登录"}）
curl -s https://zuowen.yingyuzuowen.asia/api/essay/config

# 原有系统不受影响（期望 200）
curl -s -o /dev/null -w "%{http_code}\n" https://notice.yingyuzuowen.asia/
curl -s -o /dev/null -w "%{http_code}\n" https://comment.yingyuzuowen.asia/
```

注册一个账号，免费 10 次，用一篇真作文测试 OCR 和批改。

## 7. 生成闲鱼卡密（管理员）

```bash
curl -s -X POST https://zuowen.yingyuzuowen.asia/api/admin/essay-cards \
  -H "x-token: 管理员密码" -H 'Content-Type: application/json' \
  -d '{"count":20,"credits":50,"note":"闲鱼第一批"}'
```

返回的卡密列表存好，闲鱼买家付款后发一张即可，每张只能兑换一次，服务器端核销。

## 8. 回滚（仅当出问题）

```bash
cd $APP_DIR
cp releases-backup/server.js.最新时间戳 server.js
cp releases-backup/db.js.最新时间戳 db.js
systemctl restart classroom-broadcast
```

（新表建了也不影响回滚，老代码不读这些表。）
